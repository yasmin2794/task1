import 'package:flutter/material.dart';

class Estimates extends StatelessWidget {
  const Estimates({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Estimates Section'),
    );
  }
}
