import 'package:flutter/material.dart';

class Contra extends StatelessWidget {
  const Contra({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Contra Section'),
    );
  }
}