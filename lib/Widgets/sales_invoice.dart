import 'package:flutter/material.dart';

class SalesInvoice extends StatelessWidget {
  const SalesInvoice({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sales Invoice Section'),
    );
  }
}
