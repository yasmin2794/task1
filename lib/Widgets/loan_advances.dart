import 'package:flutter/material.dart';

class LoanAdvances extends StatelessWidget {
  const LoanAdvances({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Loan/Advances Section'),
    );
  }
}
