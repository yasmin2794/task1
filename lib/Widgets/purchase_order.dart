import 'package:flutter/material.dart';

class PurchaseOrder extends StatelessWidget {
  const PurchaseOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Purchase Order Section'),
    );
  }
}
