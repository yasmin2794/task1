import 'package:flutter/material.dart';

class ESI extends StatelessWidget {
  const ESI({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ESI Section'),
    );
  }
}