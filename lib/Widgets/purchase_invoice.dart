import 'package:flutter/material.dart';

class PurchaseInvoice extends StatelessWidget {
  const PurchaseInvoice({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Purchase Invoice Section'),
    );
  }
}
