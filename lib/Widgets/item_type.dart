import 'package:flutter/material.dart';

class ItemType extends StatelessWidget {
  const ItemType({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Item Type Section'),
    );
  }
}
