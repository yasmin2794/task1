import 'package:flutter/material.dart';

class ProForma extends StatelessWidget {
  const ProForma({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pro-Forma Section'),
    );
  }
}
