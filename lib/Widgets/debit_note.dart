import 'package:flutter/material.dart';

class DebitNote extends StatelessWidget {
  const DebitNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Debit Note Section'),
    );
  }
}
