import 'package:flutter/material.dart';

class StockOpening extends StatelessWidget {
  const StockOpening({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Stock Opening Section'),
    );
  }
}
