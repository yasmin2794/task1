import 'package:flutter/material.dart';

class Ledger extends StatelessWidget {
  const Ledger({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ledger Section'),
    );
  }
}
