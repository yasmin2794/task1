import 'package:flutter/material.dart';

class Salary extends StatelessWidget {
  const Salary({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Salary Section'),
    );
  }
}