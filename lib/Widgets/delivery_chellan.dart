import 'package:flutter/material.dart';

class DeliveryChellan extends StatelessWidget {
  const DeliveryChellan({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Delivery Chellan Section'),
    );
  }
}
