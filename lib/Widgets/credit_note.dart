import 'package:flutter/material.dart';

class CreditNote extends StatelessWidget {
  const CreditNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Credit Note Section'),
    );
  }
}
